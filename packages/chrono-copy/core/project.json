{
    "name": "@focused-ux/chrono-copy-core",
    "$schema": "../../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/chrono-copy/core/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "@nx/esbuild:esbuild",
            "outputs": ["{options.outputPath}"],
            "options": {
                "main": "packages/chrono-copy/core/src/index.ts",
                "outputPath": "packages/chrono-copy/core/dist",
                "outputFileName": "index.js",
                "tsConfig": "packages/chrono-copy/core/tsconfig.json",
                "format": ["esm"],
                "external": ["vscode", "tsyringe", "@focused-ux/shared-services"]
            },
            "dependsOn": ["build-types"]
        },
        "build-types": {
            "executor": "nx:run-commands",
            "outputs": ["{projectRoot}/dist"],
            "options": {
                "command": "tsc -p tsconfig.json --emitDeclarationOnly --pretty",
                "cwd": "packages/chrono-copy/core"
            }
        },
        "check-types": {
            "executor": "@nx/js:tsc",
            "options": {
                "main": "packages/chrono-copy/core/src/index.ts",
                "tsConfig": "packages/chrono-copy/core/tsconfig.json",
                "noEmit": true
            }
        },
        "lint": {
            "executor": "nx:run-commands",
            "options": {
                "command": "pnpm eslint . --ext .ts",
                "cwd": "packages/chrono-copy/core"
            }
        },
        "clean": {
            "executor": "nx:run-commands",
            "options": {
                "command": "rimraf ./dist .turbo",
                "cwd": "packages/chrono-copy/core"
            }
        }
    }
}
